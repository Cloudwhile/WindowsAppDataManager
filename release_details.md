# Windows AppData Manager v0.1.0-alpha

> [!IMPORTANT]
>
> 这是面向早期体验者与贡献者的首个 Alpha 预发布版本，不建议用于无人值守或关键数据清理。清理前请备份重要数据，并逐项核对候选路径、归属依据与风险等级。当前 Windows 安装包尚未进行商业代码签名，SmartScreen 可能显示“未知发布者”；请仅从本仓库的 Releases 页面下载，并核对随附的 SHA-256 校验文件与 GitHub 构建来源证明。

## Highlights

- 扫描当前用户的 `Local`、`LocalLow` 与 `Roaming` 目录，结合内置规则、Windows 安装注册表、Appx 包、可执行文件元数据及 Authenticode 签名证据分析数据归属。
- 按缓存、日志、临时文件、配置、数据库、会话、Cookie、凭据与存档等类别评估风险，并以保守条件识别潜在卸载残留。
- 提供逐项确认的安全清理流程；执行前会重新验证路径身份、重解析点与运行中进程，并将内容移至 Windows 回收站以便恢复。
- 提供原生 Windows x64 安装包、SHA-256 校验文件和 GitHub 构建来源证明，发布流程包含 QML lint、完整构建、自动化测试以及安装和卸载冒烟检查。

## Added

- 新增扫描概览、应用管理、应用详情、清理建议和设置页面，支持应用搜索、筛选、排序及数据归属依据查看。
- 新增并行渐进扫描、实时进度与当前路径展示，以及扫描结果按应用身份同步的选中状态。
- 新增严格 JSON 规则加载与校验机制，并内置 Chrome、Chromium、Discord、Visual Studio Code、JetBrains、Windows 临时目录和 npm 缓存规则。
- 新增基于注册表、Appx、稳定可执行文件身份、Authenticode 发布者和运行进程的多源安装证据收集。
- 新增保守的潜在残留检测、安全清理候选生成、清理计划校验、逐项确认、取消操作与完成后自动重新扫描。
- 新增 Windows 回收站执行器和 SQLite 本地审计记录，用于保存逐项清理结果与失败信息。
- 新增浅色、深色和跟随系统主题，以及标准、减少和关闭三档动效偏好。
- 新增 Windows CI 与安装包发布工作流，覆盖后端、QML 模型、安装证据、孤儿检测、平台文件身份和清理安全回归测试。
- 首发版本已包含扫描期间的渐进结果更新、长状态文本滚动、清理取消收敛和按应用身份同步选择等稳定性处理。
- 首发版本已包含路径身份变化、重解析点、运行中进程、权限异常、QML 类型注册及 Windows CI 测试环境等边界场景的回归覆盖。

## Security

- 清理计划仅接纳位于允许根目录内且满足保守风险规则的候选项，拒绝越界路径、根目录、重解析点和身份不稳定对象。
- 执行前再次核对稳定路径身份与元数据指纹，并在检测到关联应用仍在运行或对象已变化时阻止清理，以降低检查与执行之间的竞态风险。
- 使用 Windows 回收站代替永久删除，并将成功、跳过与失败结果写入本地审计记录。
- 应用归属判断可验证 Authenticode 签名及发布者；签名缺失、冲突或不可信会降低证据可信度或阻断高风险判断。
- 发布工作流验证标签与检出提交一致性，校验安装包 SHA-256，并为安装包生成 GitHub 构建来源证明。

## Known Issues

- 本版本为 Alpha 预发布版本，规则覆盖范围有限；未知应用或证据不足的数据可能无法准确归属，并会保留为保守风险等级。
- Windows 安装包尚未进行商业代码签名，SmartScreen 可能显示“未知发布者”。
- 清理范围当前仅覆盖符合内置规则与安全约束的候选项，不提供任意路径删除或永久删除功能。

## Compatibility

- 支持 64 位 Windows 10 1809 或更高版本，以及 Windows 11。
- 官方发布包使用 Qt 6.9.2、MSVC 2022 和 Ninja Multi-Config 构建；从源码构建需要 CMake 3.16 或更高版本及 Qt 6.9.x MSVC 2022 64-bit。

## Contributors

- [Cloudwhile](https://github.com/Cloudwhile)

## Full Changelog

https://github.com/Cloudwhile/WindowsAppDataManager/compare/6a4992a...v0.1.0-alpha
